export function timezoneOffset(date: Date): string { const m = -date.getTimezoneOffset(); const sign = m >= 0 ? '+' : '-'; const abs = Math.abs(m); return `${sign}${String(Math.floor(abs / 60)).padStart(2, '0')}:${String(abs % 60).padStart(2, '0')}`; }
export function toOffsetIso(date: Date): string { const pad = (n: number, l = 2): string => String(n).padStart(l, '0'); return `${date.getFullYear()}-${pad(date.getMonth()+1)}-${pad(date.getDate())}T${pad(date.getHours())}:${pad(date.getMinutes())}:${pad(date.getSeconds())}.${pad(date.getMilliseconds(),3)}${timezoneOffset(date)}`; }
export function uniqueEntryDate(base: Date, existingIds: readonly string[]): Date { let candidate = new Date(base.getTime()); while (existingIds.includes(toOffsetIso(candidate))) candidate = new Date(candidate.getTime() + 1); return candidate; }
export function parseManualDateTime(date: string, time: string): Date | null { if (!/^\d{4}-\d{2}-\d{2}$/u.test(date) || !/^\d{2}:\d{2}$/u.test(time)) return null; const parsed = new Date(`${date}T${time}:00`); return Number.isNaN(parsed.getTime()) ? null : parsed; }
export function localDateInputValue(date: Date): string { const pad = (value: number): string => String(value).padStart(2, '0'); return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`; }
export function localTimeInputValue(date: Date): string { const pad = (value: number): string => String(value).padStart(2, '0'); return `${pad(date.getHours())}:${pad(date.getMinutes())}`; }
